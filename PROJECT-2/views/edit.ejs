<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Task</title>

  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f4f6f8;
      padding: 40px;
    }

    h1 {
      text-align: center;
      margin-bottom: 20px;
    }

    form {
      background: #fff;
      max-width: 400px;
      margin: auto;
      padding: 20px;
      border-radius: 8px;
      display: flex;
      flex-direction: column;
      gap: 15px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }

    input {
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }

    button {
      padding: 10px;
      background: #2563eb;
      color: #fff;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    button:hover {
      background: #1e40af;
    }
  </style>
</head>

<body>

  <h1>✏️ Edit Task</h1>

  <form action="/updateData" method="post">
    <input hidden name="id" value="<%= singleData.id %>">

    <input type="text" name="task" value="<%= singleData.task %>" placeholder="Task title">
    <input type="text" name="priority" value="<%= singleData.priority %>" placeholder="Priority">
    <input type="text" name="status" value="<%= singleData.status %>" placeholder="Status">

    <button type="submit">Update</button>
  </form>

</body>
</html>
